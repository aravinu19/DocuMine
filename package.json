{
  "name": "documine",
  "version": "1.0.0",
  "description": "It's an api to extract the primary context from the document uploaded.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Raspbian Horizon",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.18.3",
    "express": "^4.16.4",
    "express-fileupload": "^1.0.0",
    "pdf-text-extract": "^1.5.0"
  }
}
